# Possumic RS6x/7x HIF Python Host Driver
# STM32MP157 Linux + SPI

# No external dependencies for core operation.
# The spidev interface is accessed via Linux /dev/spidevX.Y
# and os.read/os.write (no Python spidev package needed).
# 
# Optional:
# pyserial>=3.5    # 仅 UART 模式需要
# smbus2>=0.4      # 仅 I2C 模式需要
