## 2026-07-27

- 创建 Possumic RS6x/7x HIF Python Host Driver v1 (STM32MP157 Linux, SPI)
- 基于 RS6x_7x_HIF_参考手册_V1.0, 实现 protocol/hif, spi transport, device, host_driver, commands, reports
- 固定: Check bit=1, Check32 往返测试 payload 0~4095 全通过

- v2 重构: 对照官方 SDK (02_SDK/psdf_sdk/.../host_driver/) 7层架构重构
  HAL→COM Bus→COM Device→LLC→TL→HIF→API 逐层复刻
  hal/os.py: threading.Semaphore/Thread/Lock 对应 FreeRTOS 信号量/任务
  bus/: 总线树 + 使用权信号量 (host_bus_t + host_bus_device_t)
  llc/: LLC_HANDLE 状态机 (IDLE→CLEAN→CACHE→DIRECT)
  hif/tl.py: 双线程模型 (transport_task 高优先级 + proc_task 普通优先级)
  hif/hif.py: HIFHandle (HifCfg, cmd_resp_sem, fragment_info, report_hdl 分组)
  api/: DevLocal 链表, Host_Driver_Init/Deinit/Device_Regist/Open/Close, General_Command
  api/commands.py: TLV 格式 mmw 命令

- 内存泄露修复 (5 个 P0 + 3 个 P1):
  P0: sem_take(timeout=0) 语义错误 (应为非阻塞, 却被当无限等待, 可能导致 cmd_request 永久阻塞)
  P0: msg_order_queue 无限增长 (append 但从不 pop, 每秒 N 条 MsgItem 泄漏)
  P0: data_sem BoundedSemaphore(10) 溢出 (第11条消息 crash transport_task)
  P1: sem_delete KeyError 崩溃 (ID 不存在时)
  P1: _thread_registry 死线程积累
  P1: device_regist 部分失败时 BUS device/semaphore 泄漏
  P1: device_close 无 oper_sem 保护
  验证: 信号量注册表 init→deinit 无泄漏 (1→4→1)

- 全面源码审查 (31个文件):
  发现: 双套 API 体系 (api/driver.py vs host_driver.py), frame.py/protocol.py 100% 重复
  未实现: 分片重组骨架(_fragment_preprocess 空), TL 重传(配置但无逻辑), I2C/UART stub
  已知 Bug: hal/store.py 句柄管理, reports.py C3 越界风险
  建议: 合并重复代码→统一 API 体系→实现分片/重传→修复 store→补 README
